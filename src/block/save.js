/**
 * Normally here would be the logic
 * for the front output. But due to the nature
 * of the Block, say having the dynamic content saved,
 * the desired way for the output is the PHP render callback.
 * That makes sure that user sees the actual data on front that
 * potentially could be edited but still having old content saved
 * into block json.
 */
